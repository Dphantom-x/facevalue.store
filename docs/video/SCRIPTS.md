# FaceValue - Video Scripts & Craft Playbook

_Generated 2026-06-14 from the ad/demo research workflow (5 research agents -> playbook -> 3 script guides). Cited creator-tool stats are directional benchmarks, not peer-reviewed._

---
# Craft Playbook (what professional ads/demos actually do)

I'll consolidate the research digests into a single CRAFT PLAYBOOK now.

# FaceValue â€” CRAFT PLAYBOOK: 30â€“60s Social Product-Demo Video

A working reference for writers producing 3 script options. Bullets only. Stats cited inline; treat creator-tool figures as directional benchmarks, not peer-reviewed.

---

## 1) Story / structure frameworks

- **PAS (Problem â†’ Agitate â†’ Solve)** â€” best for tight ads/cold opens. Order is load-bearing: agitate the cost *before* the solution or the payoff doesn't land. Ideal when the audience already knows the pain. [Omniscient]
- **Hookâ€“Storyâ€“Offer** (Brunson) â€” best for founder-led/narrative spots: first-second grab â†’ beforeâ†’conflictâ†’after â†’ an offer too good to refuse. [ClickFunnels]
- **AIDA** (Attentionâ†’Interestâ†’Desireâ†’Action) â€” for fuller awarenessâ†’conversion arcs; more top-of-funnel than PAS. [CFI/HubSpot]
- **Outcome-first, never feature-tour** â€” lead with the transformed end-state, show a feature *only inside the task it serves*, tie every feature to an outcome, never a menu option. [Puppydog/Spiel]
- **"Tell, Show, Tell"** â€” state the value â†’ demonstrate visually â†’ reinforce.
- **The converting demo spine (6 beats, with timings)** â€” Hook/context (5â€“15s: who + stakes) â†’ Problem (15â€“25s) â†’ **Outcome preview** (show "done" *before* the walkthrough, to earn the watch) â†’ one realistic end-to-end Walkthrough (not a feature list) â†’ Proof point â†’ ONE CTA. Mirrors buyer psychology: pain â†’ proof â†’ path â†’ next step. [Pepsales/UrbanSloth]
- **Evidence the spine works:** problem-first demos hold **56% avg watch vs 34%** feature-first; workflow demos convert **~2.3Ã— better** than feature-lists; **84%** of buyers say a demo persuaded them to buy; landing-page video can lift conversion up to **80%**. [Pepsales]
- **30â€“60s map:** hook (0â€“5s) â†’ stakes/pain (5â€“15s) â†’ product as answer in motion (15â€“45s) â†’ payoff + one CTA (45â€“60s). Keep ads under ~90s.
- **FaceValue spine:** open on the pain (real fan locked out by scalper bots) â†’ agitate â†’ preview the win â†’ one clean walkthrough (verify identity â†’ trust gate bounces the scalper swarm â†’ fan lands a face-value ticket) â†’ proof â†’ single CTA. "Scalpers vs. real fans" is your built-in villain/tension.

## 2) The hook (first 3 seconds)

- **~70%+ of viewers decide to stay or scroll within 3s** â€” the opening frame is the single highest-leverage element. TikTok: **63% of highest-CTR videos hook inside 3s**; videos keeping â‰¥65% at 3s reportedly earn **4â€“7Ã— more impressions**. [OpusClip/TikTok for Business]
- **Front-load the payoff** â€” if you have a "wow" shot, *start there* and explain after. Something should already be in motion when viewing begins (the brain hates incompleteness and stays to resolve it). Show product/interface/name within 2â€“3s. Delivering value in the first 15s holds **~20% higher retention**. [Swng/OpusClip]
- **Kill all preamble** â€” no "hey guys, welcome." Skipping intros: **+30â€“40% retention**. Open on a pain-point question: **+~23%** vs generic openers. [OpusClip]
- **Hook patterns that travel:** visual pattern break (whip-pan, push through a doorway) Â· sonic pattern break (crisp tap/branded sting *before* words) Â· problem ("I used to spend 2 hrsâ€¦") Â· results-first ("50 orders in a week") Â· contrarian ("you're doing this wrong") Â· open-loop question Â· in-media-res storytelling ("So I'm standing in line andâ€¦").
- **Mute test:** watch your first 3s on silent â€” if it doesn't create curiosity without sound, redo the visual.
- **UGC gate:** hook rate (% past 3s) decides distribution â€” aim **25â€“30%+**, kill anything <20%. [Hustler Marketing]

## 3) Retention, pacing & editing

- **Pattern interrupt every 3â€“5s** â€” zoom/punch-in, B-roll cutaway, text card, sound effect, angle change. Interrupts ~every 4s averaged **58% retention vs 41%** for static talking heads. [OpusClip/601MEDIA]
- **Place a deliberate interrupt at ~25â€“35s** (music drop, angle change), where attention typically drifts. [LightningIM]
- **Editing moves:** jump cuts to kill dead air; **10â€“15% punch-in zooms** for emphasis; rotate talking-head â†’ screen â†’ close-up â†’ cutaway to signal momentum. [601MEDIA/AIR]
- **Cut to music, cut on motion, vary shot length** â€” rapid cuts = urgency, longer holds let a hero detail breathe. Edit *to the beat* (Apple built Final Cut Beat Detection for exactly this). List/numbered segments land at ~one beat per second. Music supports, never competes; keep levels even. [Skillman/LWKS/Apple]
- **Optimize total watch time, not just %** â€” a 40s video at 60% retention (24s watched) beats a 20s at 70% (14s). TikTok sweet spot **21â€“34s** (~62% completion vs 48% at 60s+); keep most content <60s. [OpusClip]
- **Trim micro-pauses and dead frames** so momentum never sags.

## 4) Product-as-hero / demo craft

- **Show the REAL interface** â€” real product, real UI beats a faked pixel-perfect mock ("yes, even if it's not pixel-perfect"). [Puppydog]
- **"Show, don't tell"** is a throughline, not a beat â€” viewers retain ~95% of a video message vs ~10% of text; pair VO *with* visuals. Don't *say* "scalper bots get blocked" â€” *show* the swarm hit the trust gate and bounce, and a real fan's ticket land. [NewBreed]
- **Make the object monumental:** clean white/off-white/light backgrounds, meticulous key lighting + reflectors for gloss highlights; motion-control sliders, dollies, push-ins, slow macro detail passes. Reduce background motion whenever on-screen text appears so the eye lands where intended. [PetaPixel/9to5mac]
- **Shoot conditions that *prove* a feature** â€” Apple shot "Scary Fast" at night to showcase low-light. For FaceValue, stage the literal bot-swarm-vs-trust-gate moment so the claim is visible.
- **Seamless transitions â€” let the product carry the cut:** match-cut on a shared shape/motion vector so the camera reads as one continuous push, not an edit (Apple's product-to-location "wormhole" transitions are "hard to see where the cut is made"). [9to5mac]
- **Screen-demo technique:** move the cursor deliberately (slow = readable), pause before each click so auto-zoom lands cleanly, use a clean demo account, hide notifications/bookmarks. Auto-zoom on clicks + cursor smoothing reads far better than raw capture. [TechSmith]
- **UGC demo:** show the product in action close-up (~6â€“15s), beforeâ†’after mini-journey, then a hard CTA.

## 5) Voiceover, music & sound

- **VO tone:** confident, conversational, neutral/entertaining â€” never salesy or monotone. Speak slightly slow; leave pauses between sections. VO sets emotional tempo. [WhatAStory]
- **Emotion-first audio cue leads the scene** â€” prime mood before picture/copy (soft piano + inhale = relief; low drone = urgency). [Screenstory/Swng]
- **Music:** pick a rhythmically consistent track, duck it under voice, keep levels even â€” support, don't compete.
- **License-safe sources:** free/no-attribution â€” Pixabay Music (CC0-style), Mixkit, YouTube Audio Library filtered to "Attribution not required." Free-with-credit â€” Uppbeat (must paste the unique credit code per use or risk Content ID). Paid pro â€” Epidemic Sound, Artlist (verify the license covers commercial ads with no Content ID claims).

## 6) Captions, muted autoplay & aspect ratio

- **Design for silence â€” most viewing is sound-off:** ~85% of Facebook video watched muted (Verizon/Publicis 2019, n=5,616: **92% watch sound-off regularly**); only ~30% of sound-off views last â‰¥10s without captions; ~41% of videos are incomprehensible without sound. [Digiday/3Play]
- **Burn captions in** (don't trust platform auto-CC â€” renders inconsistently) and narrate all key info as on-screen text. Captions pay off: **+12% view time** on FB ad tests; **80%** more likely to finish a captioned video; **+8% ad recall, +10% memory, +13% brand linkage**. [3Play]
- **Aspect ratio:** **9:16 (1080Ã—1920)** native for TikTok & Reels (fills screen, no bars); for X/Twitter, **16:9 or 1:1** reads best in-feed (9:16 also supported). [Kapwing]
- **Length caps:** TikTok 3sâ€“10min (best <60s) Â· Reels â‰¤3min (best <60â€“90s) Â· FB Reels 90s Â· X 140s standard / 4h Premium+. Keep most under 60s.

## 7) Solo / scrappy production toolkit

- **Screen recording (the demo's core):** Screen Studio (Mac, auto-zoom on clicks + cursor smoothing, ~$9/mo) Â· free/cross-platform: Cursorful (Chrome, auto-follow + zoom, no signup), OBS (raw capture), FocuSee (AI smart-focus zoom). Record at **60 fps** for crisp motion (30 fps fine for slow UI), capture native res, **downscale to 1080p on export**.
- **Lighting:** one key light at 45Â°, slightly above eye level, diffused â€” don't pile on lights; two-light kit covers key+fill; ring light/window is the budget fallback. **Buy fanless lights** (fan noise gets into a close mic).
- **Audio (matters more than video):** soft, quiet room (curtains/rugs/furniture kill echo); a close mic in a treated space beats an expensive mic in a bare room.
- **Teleprompter (eye contact):** Kapwing / VEED (browser, free, recorder built in; VEED has AI eye-contact correction) Â· QPrompt (free desktop, commercial-use OK).
- **B-roll:** plan it *before* shooting; capture close-ups, product-in-use, cutaways to hide edits. Hold each shot longer than needed with still seconds before/after for clean in/out points; keep pans/push-ins intentional and stabilized. Overshoot â€” unused B-roll is free, missing B-roll forces a bad cut.
- **Captioning:** CapCut (free AI captions, 23 langs, animated styles) Â· DaVinci Resolve (free, AI speech-to-text + SRT) Â· Submagic/Kapwing for stylized short-form. Always proofread auto-output for product names/jargon.
- **Edit workflow (free):** DaVinci Resolve (master edit) or CapCut (faster social cut) â†’ assemble screen-recording â†’ drop in talking-head + B-roll cutaways â†’ auto-caption â†’ music bed ducked under voice â†’ export 1080p, matching frame rate.

## 8) Story-driven / UGC / founder-POV craft + pitfalls

- **Founder as storyteller = trust:** **70% prefer buying when the founder tells the story**; founders are the only ones who know the origin story. "Go Direct" â€” the personal account is the distribution channel *before* the brand launch. [FounderCoHo]
- **Native > polished** â€” UGC must read as a peer recommendation, not an ad ("imperfect > polished" on TikTok; platforms reward direct, sound-off-legible problem/solution).
- **Provocation/tension as hook (the Cluely playbook):** open on a relatable *failure*, not a feature list (Cluely: ~7.8M views, $15M Series A). Built-in villain/tension travels â€” for FaceValue, "scalpers vs. real fans" gives you the tension *without* the rage-bait trust tax.
- **Grounded, not magic** â€” show the product as a real tool with limits; blocks "hype" dismissal and keeps it relatable.
- **Volume + repost swarm** â€” Wispr Flow hit ~500M views via 70â€“80 micro-creators, **75% given specific tested hooks, 15% full creative freedom**, monitoring spikes to double down. Cheap can win: Dollar Shave Club **$4,500 â†’ ~22M views**; strong emotion + real story beats production value.
- **Situational structures that travel:** split-screen "live test" (winner by ~second 6) Â· "problem test" (5s of life-without, then the fix) Â· viewer-avatar skit (light roast of a recognizable user type â†’ shares).
- **CTA placement:** **mid-roll CTAs convert 16.95% vs 10.98% post-roll** â€” middle beats end (Wistia, 36,000+ CTAs). For short-form, put a verbal + on-screen CTA *before* the natural drop-off, not only at the very end. Use action verbs ("Get," "Claim," "Join the drop"); personalized CTAs convert **~202% better** than generic. End on a single specific action â€” "Start free" / "Join the drop" beats "Learn More." [Wistia/HubSpot]
- **Pitfalls / brand-safety traps:**
  - **Rage-bait's hangover:** 73% report *lower trust* in inflammatory brands; 76% prefer brands that avoid controversy; 62% will stop buying over perceived unethical behavior; 83% of B2B buyers audit brand social content; platforms cut inflammatory reach ~34% (2024â€“25). For a *trust/identity* brand, rage-bait is self-defeating. [Accio/TIME]
  - **Substance gap:** anchor every provocative claim to something defensible â€” Cluely's cheating claims were disproven and flipped the narrative. (FaceValue's claim is provable on-screen: show the gate actually bounce the bots.)
  - **Skeletons get exhumed** â€” vet any talent/claims (e.l.f.'s Matt Rife ad pulled over a resurfaced joke).
  - **Don't fake a persona you can't sustain** â€” awkward founder trial-and-error costs credibility; not every founder is the on-camera character.

---

**Net for FaceValue:** Hook on tension/relatable failure in <3s (fan locked out) â†’ outcome-preview the win â†’ one native, real-interface walkthrough showing the trust gate bounce the scalper swarm and a real fan land a face-value ticket â†’ proof â†’ one mid-or-end CTA ("Join the drop"). Sound-off-legible, burned-in captions, 9:16, under 60s. Borrow Cluely's *structure* (tension, villain, distribution) but anchor it to the provable claim and a positive moral frame â€” your brand's whole premise is trust, so earn it on screen.

---
# A-polished-product-film

All facts check out against the real app: `/simulation`, `/fan`, `/door`, `/drops`, `/tickets`, `/studio` all exist; agent IDs `25459` (ALLOW) and `1226` (DENY) live in `src/app/simulation/page.tsx` and `src/lib/valiron.ts`; `npm run warm` and the reset route are real. Here is the guide.

---

# FaceValue Promo Video â€” Production Guide
## CONCEPT A â€” "It just works"

---

## 1. CONCEPT

- **Name:** "It just works." â€” a Google/Apple-style polished product film.
- **One-line vibe:** Stark monochrome calm. Controlled chaos for 4 seconds, then the noise stops and the product simply *works* â€” verify once, the seat is yours, you walk in, green check.
- **Goal it optimizes:** **CREDIBILITY** (not raw reach). This is the deliverable owed to the Valiron founders and it is aimed at an agent-identity / founder / investor audience asking "is this a real company?" The whole film is built to answer that with a real interface and a real, unscripted trust call â€” not hype.
- **Recommended length: 45 seconds.** The playbook's 30â€“60s map and the "optimize total watch time, not just %" rule both point here: a 45s film at high retention beats a rushed 20s cut (a 40s/60% video = 24s watched beats a 20s/70% = 14s). 45s gives the live Valiron beat room to breathe (it needs ~6â€“10s to read as real) while staying under the 60s social ceiling. Keep ads under ~90s â€” we are well inside.
- **Aspect ratio: 9:16 master (1080Ã—1920), with a 1:1 alt export for the X in-feed post.** The film's true home is Reels/TikTok where 9:16 fills the screen with no bars. For X specifically the playbook says 16:9 or 1:1 reads best in-feed â€” and a centered, monochrome product film crops cleanly to 1:1. So: **shoot/record at 1440px wide, master 9:16, export a 1:1 for the tweet.** (Frame the action in a center-safe column so both crops work from one edit.)

---

## 2. WHY IT WORKS

- **Outcome-first, never a feature-tour** (Puppydog/Spiel; the "converting demo spine"). We show the transformed end-state â€” the green check at the door â€” and we lead the body with the *win*, not a menu of features. Every screen appears only inside the task it serves (verify â†’ claim â†’ ticket â†’ scan).
- **Front-load the payoff / pattern break in <3s** (OpusClip/TikTok: ~70% decide in 3s; videos holding â‰¥65% at 3s earn 4â€“7Ã— impressions). The 4-second chaos cold-open is a visual *and* sonic pattern break â€” a price counter climbing $60 â†’ $1,200 is "already in motion," and the brain stays to resolve the incompleteness.
- **Show, don't tell + product-as-hero** (NewBreed: ~95% retention of video vs ~10% of text; Puppydog: "show the REAL interface"). We never *say* "scalpers are blocked" â€” we *show* the actual `/simulation` trust gate return DENY on the scalper agent and ALLOW on the verified fan, live and labeled "Not scripted." This is exactly the playbook's "anchor every provocative claim to something defensible" (the Cluely substance-gap lesson): our claim is provable on screen.
- **Calm/grounded over rage-bait, for a trust brand** (Accio/TIME: 73% report lower trust in inflammatory brands; 76% prefer brands that avoid controversy). FaceValue is the ANTI-cheating brand â€” a premium, confident product film is on-brand in a way rage-bait would actively undermine. The tension comes from the built-in villain (scalpers losing), framed as the fan winning *fairly*.

---

## 3. THE HOOK (first 3 seconds)

- **Exact first frame:** Pure black for ~3 frames, then SNAP to a tight, stark macro of a resale price readout mid-climb â€” big monochrome numerals **`$1,040`** ticking upward, a queue-position counter (`You are #14,213 in line`) blurred behind it, and a faint **"SOLD OUT"** stamp ghosting in. Everything is moving the instant the frame appears. No logo yet, no preamble.
- **On-screen text (verbatim):** `$60 face value` (small, top) â†’ the big number rolls `$1,040â€¦ $1,180â€¦ $1,200`.
- **Why it stops the scroll:** It is a **results-/problem-first hook with something already in motion** (OpusClip: front-load the payoff; the brain hates incompleteness and stays to resolve it). A price visibly racing from $60 toward $1,200 is the emotional anchor (the real $60â†’$1,200 scalp, the Eras-Tour meltdown) compressed into one unresolved number. It passes the **mute test** â€” it creates "wait, what?" with zero sound â€” and it kills all preamble (+30â€“40% retention vs an intro).

---

## 4. SHOT-BY-SHOT

Runtime 0â€“45s. All captions are burned-in, center-safe column. VO is calm and unhurried (see Â§6).

| # | Time | Shot / Visual (real FaceValue screens referenced) | On-screen caption (verbatim) | Voiceover line (verbatim) |
|---|------|---|---|---|
| 1 | 0â€“2s | Cold open. Macro of resale price counter racing upward; blurred queue position + faint "SOLD OUT" behind. Hand-held micro-jitter, harsh. | `$60 face value` then big rolling `$1,040 â†’ $1,200` | *(silence â€” let the chaos play)* |
| 2 | 2â€“4s | Fast 3-cut montage of the pain: a "Queue: 14,213 ahead of you" bar, a **crash/error** screen ("Something went wrong"), the price stamp locking on `$1,200`. Cuts get faster. | `Today, the bots win.` | *(silence / rising room-tone)* |
| 3 | 4â€“5s | HARD CUT TO BLACK. Everything stops. One beat of true silence. | *(none)* | *(beat of silence)* |
| 4 | 5â€“7s | The FaceValue mark draws on in white line â€” the face-profile "F" with ticket serrations â€” and settles. Centered, premium. | `FaceValue` (small, under mark) | "Tickets â€” the way they should work." |
| 5 | 7â€“11s | Smooth push into the real `/drop/[id]` page, drop pre-open, countdown visible. Calm. Cursor moves slowly. | `One human. One ticket.` | "No queue. No bots. No resale markup." |
| 6 | 11â€“16s | The **locked onboarding wizard** overlay on `/drop/[id]`: step 1 Account âœ“, then **step 2 "verify you're one human"** highlighted. World ID / Valiron check resolves to a clean checkmark. | `Verify once â€” you're one real human.` | "You verify you're one real human. Once." |
| 7 | 16â€“19s | Step 3 Payment confirms; the wizard closes. Countdown hits zero â€” **the drop opens**. A subtle "Claimingâ€¦" state on the seat. | `The drop opens.` | "Then the drop opens â€”" |
| 8 | 19â€“22s | The seat resolves to **claimed in the background** â€” a clean "Seat secured Â· face value" confirmation. No frantic clicking shown; it just happens. | `Your verified agent claims your seat.` | "â€” and your verified agent claims your seat. In the background." |
| 9 | 22â€“25s | Cut to `/tickets`: the fan's ticket on a phone frame, **HMAC-signed QR** rendering crisply. Slow macro pass over the QR. | `Bound to you. Can't be flipped.` | "The ticket is bound to you. It can't be bulk-bought, and it can't be resold." |
| 10 | 25â€“34s | **THE MAGIC MOMENT** â€” cut to `/simulation`. Quick read of the two grids (Mode A scalpers take ~36/40; Mode B all seats to verified fans), then push DOWN to the two live cards: verified-fan agent `25459` â†’ **ALLOW** (score 92, World ID), scalper `1226` â†’ **DENY** (sandboxed). The "Not scripted" label is on screen. (Full staging in Â§5.) | `This is the actual trust gate. Not scripted.` | "This isn't a mockup. Two real agents, checked live: the real fan is allowed â€” the scalper is denied." |
| 11 | 34â€“38s | Cut to `/door` (staff scan). A valid QR scans â†’ big **green check**. Then the *same* ticket scans again â†’ **"Already used."** | `Scanned once. Then it's spent.` | "At the door: one scan, one entry. Try to reuse it â€” it's already spent." |
| 12 | 38â€“42s | Payoff card on stark black: five seat icons resolve to five fan silhouettes, zero scalper marks. Monochrome, confident. | `5 tickets â†’ 5 real fans â†’ 0 scalpers.` | "Five tickets. Five real fans. Zero scalpers." |
| 13 | 42â€“45s | The FaceValue mark + tagline + "Built on Valiron" lockup. One CTA. Hold, clean. | `Real fans. Real tickets. Face value.` / `Built on Valiron` / `facevalue.store` | "FaceValue. Built on Valiron." |

---

## 5. THE MAGIC MOMENT (the live Valiron call)

This is the credibility centerpiece â€” it must read as *real* in ~6â€“10s (shots 10, 25â€“34s). Stage it on the **real `/simulation` page**, which already fires live calls.

- **Screen & framing:** Start on `/simulation` framed so both grids are visible for ~2s â€” just long enough to read "Mode A: scalpers take ~36/40" vs "Mode B: every seat to a verified fan." Don't linger on the grids; they're context.
- **The push:** A single smooth **push-down** (motion-control feel â€” slow, continuous, reads as one camera move, not a cut â€” per the Apple "let the product carry the cut" technique) from the grids to the **two live trust cards** below.
- **What to highlight (auto-zoom / punch-in on each in turn):**
  - Card 1 â€” **verified fan, agent `25459`**: punch in as it returns **ALLOW**, trust score **92**, "World ID verified." Let the result *land* visibly (pause before it resolves so the change is legible).
  - Card 2 â€” **scalper, agent `1226`**: punch in as it returns **DENY / sandboxed**.
  - The contrast ALLOW-green vs DENY is the whole point â€” frame them so both verdicts are on screen together at the end of the beat.
- **The caption that sells it (verbatim, persistent through the beat):** `This is the actual trust gate. Not scripted.` (mirror the app's own on-screen label "Not scripted â€” this is the actual trust gate").
- **Why it reads real:** millisecond-ish latency and the on-screen agent IDs/scores are things a faked mock wouldn't bother with â€” "show the REAL interface, even if it's not pixel-perfect" (Puppydog). Reduce all background motion while the verdict text appears so the eye lands exactly on ALLOW/DENY (PetaPixel).
- **CRITICAL production note:** the Valiron edge proxy cold-starts 30â€“60s. **Run `npm run warm` and confirm a warm response before you hit record**, then capture this beat *first* while it's hot. Filmed cold, the live call just hangs on camera and the magic moment dies. If a take stalls, re-warm and re-shoot â€” never ship a hang.

---

## 6. FULL VOICEOVER SCRIPT

**Tone/delivery:** Calm, confident, unhurried â€” the "it just works" register. Speak slightly slow, leave a real pause after "Once." and at the cut-to-black. Never salesy, never hyped. Think premium product narrator, not a pitch. (Brother narrating: aim for warm-neutral, low energy, total certainty.)

> *(0â€“4s: silence over the chaos)*
>
> Tickets â€” the way they should work.
>
> No queue. No bots. No resale markup.
>
> You verify you're one real human. Once.
>
> Then the drop opens â€” and your verified agent claims your seat. In the background.
>
> The ticket is bound to you. It can't be bulk-bought, and it can't be resold.
>
> This isn't a mockup. Two real agents, checked live: the real fan is allowed â€” the scalper is denied.
>
> At the door: one scan, one entry. Try to reuse it â€” it's already spent.
>
> Five tickets. Five real fans. Zero scalpers.
>
> FaceValue. Built on Valiron.

*(~80 words â‰ˆ 38â€“40s of calm VO over a 45s cut, leaving the 4s cold-open and the end-card breathing room â€” by design.)*

---

## 7. MUSIC & SOUND DESIGN

- **Overall arc:** tension â†’ release. The cold open is *abrasive* (chaos), the cut-to-black is a held silence, and the body is a clean, confident minimal bed. **Emotion-first audio cue leads each phase** (Screenstory/Swng): the silence-then-tone at the cut primes "relief."
- **Track style (royalty-free references):**
  - Body/hero (5â€“45s): a **minimal, monochrome electronic-piano or muted-pulse bed** â€” even, rhythmically consistent, ducked under VO. Search **Pixabay Music** / **Mixkit** for "minimal corporate piano," "clean tech ambient," "premium product reveal." (Pixabay = CC0-style, no attribution; safest for an ad.)
  - If you want polish and have budget: **Epidemic Sound / Artlist** "minimal tech / Apple-style" â€” verify the license covers commercial ads with no Content ID claims.
  - Avoid **Uppbeat** unless you paste the unique per-use credit code (Content ID risk).
- **SFX (sparse, deliberate):**
  - **0â€“4s:** layered UI-error blips + a rising **riser** under the climbing price (tension).
  - **4â€“5s (cut to black):** a single low **impact/sub-hit**, then ~0.5s of true silence (the sonic pattern break that buys the watch).
  - **5â€“7s (logo draw):** a soft, crisp **branded sting** as the mark resolves â€” *before* the first VO word.
  - **Walkthrough:** subtle, real **UI click/confirm ticks** on each step (verify âœ“, seat secured, claim) â€” low and even.
  - **Magic moment:** a clean "confirm" tone on **ALLOW**, a muted "reject" thud on **DENY** â€” make the verdict audible *and* visual.
  - **Green check at door:** a single bright confirm chime.
- **Mixing:** music supports, never competes; duck under voice; keep levels even (Skillman/LWKS). Cut transitions on the beat where possible (the walkthrough confirms can land on downbeats).

---

## 8. CAPTIONS SPEC

- **Style:** burned-in (do NOT trust platform auto-CC â€” it renders inconsistently). Monochrome to match brand: **white text, medium-weight sans (match the app's UI font), on a subtle dark scrim** where it overlaps bright UI. Tight, short lines â€” 3â€“6 words. No animated bounce; clean fades that match the premium tone.
- **Placement:** center-safe **lower-third**, kept inside the 1:1 crop box so it survives both the 9:16 master and the 1:1 X export. Keep captions clear of the phone-frame QR and the `/simulation` verdict text.
- **Key-info-as-text rule:** every load-bearing claim is on screen as text, not just spoken â€” especially `This is the actual trust gate. Not scripted.`, `Bound to you. Can't be flipped.`, and `5 tickets â†’ 5 real fans â†’ 0 scalpers.`
- **Muted-legibility check (must pass with zero sound):** ~85% of feed video is watched muted; ~41% of videos are incomprehensible without sound. Watch the cut on silent end-to-end â€” a viewer should understand the entire story from captions + UI alone: *price explodes â†’ FaceValue â†’ verify once â†’ seat claimed â†’ QR bound to you â†’ trust gate denies scalper / allows fan â†’ green check â†’ 5 fans, 0 scalpers â†’ Built on Valiron.* If any beat is unclear muted, the caption is wrong, not the VO. **Proofread auto-captions for product names** ("Valiron," "FaceValue," agent IDs) â€” auto-tools mangle these.

---

## 9. ASSETS & GEAR

**Screen-record (the core of the film â€” record at 1440px wide, 60fps, native res, downscale to 1080p on export):**
- `/drop/[id]` â€” pre-open countdown + the **locked onboarding wizard** (step 1 Account, step 2 verify-one-human, step 3 Payment), then drop-opens + "seat secured."
- `/tickets` â€” the claimed ticket with the **HMAC-signed QR**.
- `/simulation` â€” both grids (Mode A vs Mode B) **and** the two live trust cards (`25459` ALLOW / `1226` DENY) â€” capture this **first, while Valiron is warm**.
- `/door` â€” valid scan â†’ green check, then the same ticket â†’ "Already used."
- *(Optional B-roll: `/drops` list and `/studio` audit feed for a flash of "real product surface area," if a cut needs filler.)*

**Camera / shot (minimal â€” this is a screen film, not a talking-head):**
- No on-camera presenter required. Optionally shoot **one real-world cutaway**: a phone in-hand walking up to a venue door, screen showing the FaceValue ticket, for shots 9/11. One key light at 45Â°, diffused; window light is a fine fallback.

**The cold-open chaos (shots 1â€“2):** build as **motion graphics**, not real capture â€” a monochrome price counter animating $60â†’$1,200, a queue-number bar, a generic "Something went wrong" error card. Easiest in CapCut/After Effects/Resolve Fusion; keep it stark B/W to match brand.

**Solo-friendly tools:**
- **Screen recording:** Screen Studio (auto-zoom on clicks + cursor smoothing â€” ideal for the deliberate-cursor demo) or free Cursorful/OBS. Record **60fps**, native res, **downscale to 1080p**.
- **Edit:** DaVinci Resolve (master, free) or CapCut (faster social cut).
- **Captions:** CapCut AI or Resolve speech-to-text â†’ **proofread** product names.
- **Audio:** brother's VO recorded with a close mic in a soft/quiet room (curtains/rugs kill echo â€” a treated room matters more than an expensive mic). Fanless lighting if any camera is used.

---

## 10. EDITING & MOTION NOTES

- **Two pacing zones, deliberately opposed.** Cold open (0â€“4s): fast, harsh, accelerating jump-cuts to manufacture tension. Body (5â€“45s): **slow, confident holds** â€” long enough to let each hero detail (QR macro, the ALLOW verdict) breathe. The contrast *is* the "it just works" feeling.
- **The cut-to-black at 4â€“5s is the single most important edit** â€” a full stop + held silence. This is your deliberate pattern interrupt and the hinge between chaos and calm. Don't rush it.
- **Pattern interrupt every 3â€“5s** (OpusClip: ~58% vs 41% retention): each walkthrough step is its own beat with a punch-in or a confirm tick; the `/simulation` push-down and the door-scan green check are built-in interrupts. Place the deliberate mid-roll interrupt right at the **magic moment (~25â€“34s)**, exactly where attention drifts.
- **Transitions â€” let the product carry the cut** (Apple/9to5mac): use **match-cuts on shared motion** â€” e.g., the push *into* `/drop/[id]` continues the camera's forward motion from the logo reveal; the push-*down* on `/simulation` reads as one continuous move. Avoid flashy wipes; this is a premium film.
- **Screen-demo craft** (TechSmith): move the cursor **slowly and deliberately**, pause ~0.3s before each click so auto-zoom lands cleanly, use a **clean demo account** (run the reset), hide notifications/bookmarks/extra tabs.
- **Cut on motion, cut to the beat** (Skillman/LWKS): land the verify âœ“, the seat-secured confirm, and the green check on musical downbeats.
- **Reduce background motion whenever on-screen text appears** so the eye lands where intended (PetaPixel) â€” especially under the `Not scripted` caption.

---

## 11. PRODUCTION CHECKLIST

**Pre-roll (in order):**
1. **`npm run warm`** â€” wake the Valiron edge proxy and **confirm a warm/successful response** (it cold-starts 30â€“60s; a cold call hangs on camera). Re-warm if you pause filming for a while.
2. **`POST /api/dev/reset`** â€” reset to a clean demo state (fresh DB, no stale users/tickets/holds).
3. **`npm run dev`** â€” start the Next dev server. (Remember `next.config.ts` needs `serverExternalPackages: ["better-sqlite3"]`; if DB routes 500, restart the dev server after any config change.)
4. Browser prep: **1440px-wide window**, hide bookmarks bar, close extra tabs, disable notifications, clear any extension chrome. Use a clean seeded vendor/fan as needed.
5. Pre-stage data so the demo is clean: have a drop ready on `/drops`, and confirm `/simulation` returns the expected `25459` ALLOW / `1226` DENY.
6. **Capture the magic moment FIRST** (`/simulation` live call) while Valiron is hot. Then record the rest of the walkthrough.
7. Pre-warm a successful **door scan** path so you can film the green check and the "already used" repeat cleanly.

**Record settings:**
- Screen capture **1440px wide, 60fps, native resolution; downscale to 1080p on export.**
- Master timeline **9:16 (1080Ã—1920)**; export a **1:1** alt for the X post. Keep all action in the center-safe column so both crops work from one edit.
- Audio: record VO separately (close mic, quiet room), sync in the edit; export with music ducked under voice, even levels.

---

## 12. COMPANION TWEET

> Tickets â€” the way they should work.
>
> One verified human = one ticket. Bound to you. Can't be bulk-bought, can't be resold.
>
> The trust gate in this clip is live, not scripted: real fan â†’ allowed, scalper â†’ denied.
>
> Real fans. Real tickets. Face value.
> Built on @valiron ðŸ–¤
>
> facevalue.store

**Tags / handles:**
- Tag the Valiron founders **@vatsa_shah / @drew_mailen** *(verify exact handles before posting â€” confirm against their profiles)* and **Valiron's own account** (confirm the handle â€” written as @valiron above as a placeholder).
- Post from / credit the founder's Twitter **@D_phantom_x**.
- **Handle caveat â€” DO NOT post until verified:** IG is given as **@facevalue_us** (underscore) but **facevalue.us** (dot) appears elsewhere â€” **unconfirmed**. Confirm the exact IG handle and the exact Valiron + founder handles before the tweet goes live. Keep the domain as **facevalue.store** (confirmed).
- Keep tweet copy within X's standard limit; the video is the hero, copy is support. One CTA only (the domain).

---

## 13. DIFFICULTY & RISK

- **Solo founder time estimate: ~1.5â€“2.5 days.**
  - Cold-open motion graphics (price counter, queue, crash card): ~3â€“5 hrs.
  - Screen captures (warm + reset + record all five surfaces, multiple takes): ~3â€“4 hrs.
  - VO record + sync (brother): ~1 hr.
  - Edit, transitions, captions, music, mix, 9:16 + 1:1 exports: ~6â€“10 hrs.
  - Buffer for re-warming/re-shooting the live call: build it in.
- **Single biggest failure mode: the live Valiron call filmed cold â†’ it hangs on camera and the credibility centerpiece dies.** This is the one beat that can't be faked (faking it would betray the entire trust premise â€” the Cluely substance-gap lesson). **Guard:** always `npm run warm` and confirm a warm response immediately before recording `/simulation`, shoot that beat first, and if any take stalls, stop, re-warm, and re-shoot â€” never ship a hang and never "recreate" the verdict in post.
- **Secondary risk:** posting before verifying handles (Valiron founders, Valiron account, IG dot-vs-underscore) â€” a wrong @ on the deliverable owed to the founders is an avoidable own-goal. Verify all tags before publishing.

---

**File references (all absolute):**
- `C:\Users\aldom\Desktop\antigravity\facevalue\src\app\simulation\page.tsx` â€” the magic-moment screen (live `25459` ALLOW / `1226` DENY).
- `C:\Users\aldom\Desktop\antigravity\facevalue\src\app\drop\[id]\` â€” locked onboarding wizard + claim.
- `C:\Users\aldom\Desktop\antigravity\facevalue\src\app\tickets\page.tsx` â€” QR ticket.
- `C:\Users\aldom\Desktop\antigravity\facevalue\src\app\door\page.tsx` â€” scan â†’ green check / already-used.
- `C:\Users\aldom\Desktop\antigravity\facevalue\src\lib\valiron.ts` â€” normalized trust result (ALLOW/DENY logic + agent IDs).
- `C:\Users\aldom\Desktop\antigravity\facevalue\scripts\warm.ts` â€” `npm run warm` (run before filming the live call).

---
# B-the-fusion

I have everything verbatim. The door beat is exact: first scan â†’ `âœ“ Checked in â€” [event]` (green), second scan â†’ `Already checked in â€” duplicate scan.` (red). That's the perfect "bound to identity, can't be flipped" visual. The claim success is `Ticket secured at face value $X â€” bound to your verified identity, non-transferable.`

All real strings confirmed. No invented features. Here is the complete production guide.

---

# FaceValue X Promo â€” Production Guide: CONCEPT B "The Fusion"

## 1. CONCEPT

- **Name:** "The Fusion" â€” a Cluely-style relatable-failure hook welded onto a Google-clean proof core.
- **One-line vibe:** *"You got shut out. Watch the gate that stops it â€” live, on camera, not scripted."*
- **Goal it optimizes:** **Credibility first, reach second** â€” but engineered to win both. This is the deliverable owed to the Valiron founders for their X feature, so the back half must make **Valiron visibly load-bearing**; the front half (the hook) buys the reach so the proof gets watched. Per the playbook: *"The hook buys the watch; the polished core earns the belief."*
- **Recommended length: 45 seconds.** Playbook: *"a 40s video at 60% retention (24s watched) beats a 20s at 70% (14s)"* â€” optimize total watch time, not just percentage. 45s gives the live Valiron call room to breathe (the magic moment needs ~8s legibly) while staying under the *"keep most content <60s"* ceiling and inside X's 140s standard limit with margin.
- **Aspect ratio: 1:1 (1080Ã—1080) as the master, with a 9:16 (1080Ã—1920) safe-area crop.** Playbook: *"for X/Twitter, 16:9 or 1:1 reads best in-feed."* 1:1 maximizes feed real estate in the X timeline (where Valiron's founders + their audience live) without the letterboxing 16:9 gets on mobile. Shoot/record at 1440px wide and crop so a 9:16 Reels/TikTok repost is free later (the split-screen `/simulation` is the only shot that needs a 16:9 alt â€” see Â§9).

---

## 2. WHY IT WORKS

- **Provocation-as-hook, anchored to a provable claim (the Cluely playbook).** Playbook: *open on a relatable failure, not a feature list; "built-in villain/tension travels â€” for FaceValue, 'scalpers vs. real fans' gives you the tension without the rage-bait trust tax."* We open on the fan getting shut out (or the scalper raking seats), then immediately ground the provocation in something defensible â€” the `/simulation` page literally labeled **"Not scripted â€” this is the actual trust gate."** This dodges the **substance gap** pitfall (*"Cluely's cheating claims were disproven and flipped the narrative"*); ours is true on screen.
- **Outcome-preview before the walkthrough earns the watch (the converting demo spine).** Playbook: *"show 'done' before the walkthrough, to earn the watch... problem-first demos hold 56% avg watch vs 34% feature-first."* By second ~9 the viewer has already seen the win-state (every seat green, "Resale: 0") before we walk the product.
- **"Show, don't tell" with the real interface (product-as-hero).** Playbook: *"Show the REAL product, real UI beats a faked mock... viewers retain ~95% of a video message vs ~10% of text. Don't say 'scalper bots get blocked' â€” show the swarm hit the trust gate and bounce."* Every claim is demonstrated on the working Next.js app: the **DENY** card firing live, the door scan flipping to **"Already checked in â€” duplicate scan."**
- **Founder-told story + the live test = trust, which is the entire brand.** Playbook: *"70% prefer buying when the founder tells the story"* and *"split-screen 'live test' (winner by ~second 6)."* For a *trust/identity* brand, showing the gate work live IS the marketing â€” and it makes Valiron the hero, satisfying the feature deliverable.

---

## 3. THE HOOK (first 3 seconds)

- **The exact first frame:** A phone screen (vertical, held in hand, shot on camera â€” not a screen-record), mid-refresh on a generic ticket page, snapping to a hard **"SOLD OUT"** state. Freeze on the defeated half-second. No logo, no preamble, product not yet shown â€” pure relatable failure.
- **On-screen text (verbatim, burned in, top-third):**
  > **"$60 face value.**
  > **Gone in 4 seconds.**
  > **Now $1,200 resale."**
  (Three lines, each snapping in on a beat, synced to the refresh.)
- **Why it stops the scroll:** Playbook â€” *"~70%+ of viewers decide within 3s; open on a pain-point question/relatable failure: +~23% retention vs generic openers; kill all preamble (+30â€“40%)."* The $60â†’$1,200 jump is the playbook's stated **emotional anchor**; something is already in motion (the refresh resolving to SOLD OUT) so *"the brain hates incompleteness and stays to resolve it."* It passes the **mute test** â€” the price math lands with zero sound.

> Hook choice rationale: the guide uses the **defeated-fan refresh** as primary (it's the universal, sympathetic POV and the safest moral frame â€” the FAN losing fairly, never a covert-advantage angle). The scalper-wall-of-monitors is the **alt B-roll** for a second cut to A/B test; keep it as a 0.5s flash inside the hook if you have the footage, but lead with the fan.

---

## 4. SHOT-BY-SHOT

Runtime 45s. Reference screens are the **real** FaceValue app. Captions are verbatim and burned in.

| # | Time | Shot / Visual | On-screen caption (verbatim) | Voiceover line (verbatim) |
|---|------|---------------|------------------------------|---------------------------|
| 1 | 0â€“3s | **HOOK.** Phone in hand, ticket page refreshing â†’ snaps to **SOLD OUT**. Defeated freeze. (Optional 0.4s flash: scalper's wall of monitors.) | `$60 face value.` / `Gone in 4 seconds.` / `Now $1,200 resale.` | "A sixty-dollar ticket. Gone in four seconds â€” to bots." |
| 2 | 3â€“6s | Hard cut to the resale listing: same ticket, **$1,200**. Quick push-in on the price. | `Real fans get shut out.` | "Real fans get shut out. Every single drop." |
| 3 | 6â€“9s | **SNAP** (whip-pan / hard match-cut) into clean monochrome product world. **`/simulation`** loads, *"Run the drop"* clicked. Both grids start filling. | `So we built the gate.` | "So we built a ticket bots can't touch." |
| 4 | 9â€“13s | `/simulation` split-screen plays out: **Panel A** fills mostly red (scalpers), **Panel B** fills all green. Hold on the two summary lines. | `Today: scalpers grab 36/40.` / `FaceValue: 40/40 real fans. Resale: 0.` | "Today, scalpers take thirty-six of forty seats. With FaceValue â€” every seat, a verified human. Resale: zero." |
| 5 | 13â€“23s | **THE MAGIC MOMENT** (see Â§5). Scroll down `/simulation` to the two live cards. **Verified fan â†’ ALLOW (score 92, World ID âœ“)**; **scalper â†’ DENY (sandbox)**. Cursor underlines the "Not scripted" line. | `Not scripted â€” the actual trust gate.` / `ALLOW Â· 92 Â· World ID âœ“` / `DENY Â· sandbox` | "This part is not scripted. Two real agents, checked live against Valiron. The verified fan â€” allowed. The scalper â€” denied. On camera." |
| 6 | 23â€“28s | Cut to **`/drop/[id]`** with the **locked onboarding wizard** overlay. Step through the 3 pills fast: **Account â†’ One human â†’ Payment**. Land on *"Verify with World ID."* | `One human. One ticket.` / `Account â†’ Verify â†’ Pay` | "For a fan, it's one flow. Make an account, prove you're one human, add a card." |
| 7 | 28â€“32s | Wizard *"Prove you're one human"* â†’ tap verify â†’ **"You're in â€” one human, one ticket."** Then the live **Buy at face value** button; claim. | `Verified. No bots to hide behind.` | "Sixty seconds. No eye scans, no crypto â€” we never learn your name." |
| 8 | 32â€“37s | **`/tickets`** â†’ the issued **QR ticket**. Caption from the real claim result. Slow macro push on the QR. | `Ticket secured at face value â€” bound to your identity, non-transferable.` | "Your ticket. At face value. Bound to you â€” it can't be flipped." |
| 9 | 37â€“41s | **THE PROOF KICKER. `/door`** staff scan: paste token â†’ **green âœ“ "Checked in."** Then paste the SAME token again â†’ **red "Already checked in â€” duplicate scan."** | `Scan once: âœ“` / `Scan twice: âœ• already used` | "Try to resell it? The door catches the second scan. Un-scalpable." |
| 10 | 41â€“45s | Logo resolve: the **F-with-a-face / ticket-serration** mark on black. Tagline + CTA + Valiron credit. | `Real fans. Real tickets. Face value.` / `Built on Valiron` / `facevalue.store` | "FaceValue. Real fans, real tickets, face value. Built on Valiron." |

> Mid-roll CTA note (playbook: *"mid-roll CTAs convert 16.95% vs 10.98% post-roll"*): drop a small persistent **`facevalue.store`** chip in the lower corner from shot 6 onward, so the CTA is present before the natural drop-off, not only on the end card.

---

## 5. THE MAGIC MOMENT (the live Valiron call â€” 13â€“23s, ~10s on screen)

This is the deliverable's whole point: make Valiron **visibly load-bearing** and **provably real**.

- **Screen:** the bottom section of **`/simulation`** â€” the two `VerdictCard`s under the eyebrow **"Live Â· real calls to Valiron"** and the headline **"Not scripted â€” this is the actual trust gate."** (All three strings are real, on-page.)
- **Staging for legibility in ~10s:**
  1. **Show the loading state, briefly.** When *"Run the drop"* fires, both cards show *"Checking with Valironâ€¦"* with skeleton bars. **Let this sit ~1s** â€” the visible round-trip is what proves it's a real network call, not a canned graphic. (This is exactly why warming Valiron first is non-negotiable â€” see Â§11. Cold, this spinner hangs 30â€“60s and kills the take.)
  2. **Left card resolves to ALLOW (green).** Highlight, in order, with a cursor or a 12px punch-in zoom: the big **âœ“ ALLOW**, then **`25459 Â· ethereum`**, then the badge row **score 92 Â· World ID âœ“ Â· â†’ prod**. These are the real `Trust` fields the card renders.
  3. **Right card resolves to DENY (red):** **âœ• DENY**, **`1226 Â· solana`**, **sandbox**, **no World ID**.
  4. **Cursor underlines** the headline *"Not scripted â€” this is the actual trust gate."* Hold 1s. That's the belief beat.
- **What to highlight (eye path):** ALLOW âœ“ â†’ score 92 â†’ World ID âœ“ â†’ (snap right) â†’ DENY âœ• â†’ sandbox. Playbook: *"reduce background motion when on-screen text appears so the eye lands where intended"* â€” pause the grid animation above before the cards resolve.
- **Caption (burned in, sits the whole 10s):**
  > **"Not scripted â€” the actual Valiron trust gate."**
  > sub: **`Verified fan â†’ ALLOW Â· 92 Â· World ID âœ“`** / **`Scalper â†’ DENY Â· sandbox`**
- **Realness tells to keep (don't edit out):** the brief spinner, the millisecond feel of the resolve, and the real agent IDs/chains. Playbook: *"shoot conditions that prove a feature... stage the literal bot-vs-gate moment so the claim is visible."*

> Optional B-roll insert (founders will love this): a 1.5s cut to **`/fan`** â€” the **"valiron Â· engine"** terminal streaming `â†’ valiron.getAgentProfile(25459, ethereum)` then `âœ“ trust gate: ALLOW Â· score 92 Â· prod Â· World ID âœ“ (NNNms)` with the real millisecond timing. It reinforces "real call" with literal logs. Only include if it doesn't push past 10s on the moment.

---

## 6. FULL VOICEOVER SCRIPT

> **Tone/delivery direction:** Confident, conversational, slightly slow, neutral-not-salesy â€” the founder's brother narrating like he's showing a friend something that actually works. Drop the pitch entirely on the "not scripted" line; let it sound matter-of-fact, almost understated. Leave a real beat of silence after "On camera." and after "Un-scalpable." Playbook: *"VO sets emotional tempo; speak slightly slow; leave pauses between sections."*

```
A sixty-dollar ticket. Gone in four seconds â€” to bots.
Real fans get shut out. Every single drop.

So we built a ticket bots can't touch.

Today, scalpers take thirty-six of forty seats.
With FaceValue â€” every seat, a verified human. Resale: zero.

This part is not scripted. Two real agents, checked live against Valiron.
The verified fan â€” allowed. The scalper â€” denied. On camera.

For a fan, it's one flow. Make an account, prove you're one human, add a card.
Sixty seconds. No eye scans, no crypto â€” we never learn your name.

Your ticket. At face value. Bound to you â€” it can't be flipped.
Try to resell it? The door catches the second scan. Un-scalpable.

FaceValue. Real fans, real tickets, face value. Built on Valiron.
```

(~95 words â‰ˆ 42â€“44s at a slightly-slow pace, leaving the two silent beats.)

**Language-discipline check:** OUR side is never a "bot" â€” bots are only ever the scalpers' ("a ticket bots can't touch," "the scalper â€” denied"). The fan has a "verified agent" (in shot copy) operating in a sanctioned pipeline. No "crypto" marketed (we explicitly say *"no crypto"* â€” that's the wizard's own real line). No "stealth"/covert-advantage framing: every edge is the FAN winning **fairly** or the SCALPER **losing**. "Built on Valiron" closes it.

---

## 7. MUSIC & SOUND DESIGN

- **Direction:** Two-act audio arc mirroring the two-act structure. **Act 1 (0â€“6s, the pain):** a low, tense drone / single detuned pulse under the refresh â€” *"low drone = urgency"* (playbook). **The SNAP at 6s** is the hinge: a clean riser tail + a crisp branded impact lands exactly on the whip-pan into `/simulation`. **Act 2 (6â€“45s):** a minimal, confident, rhythmically-steady electronic bed (think Apple-product-film restraint â€” sparse percussion, clean synth, even levels), ducked hard under the VO. Cut visuals to its beat (playbook: *"cut to music, cut on motion"*). A subtle **music drop / new layer at ~28s** is the deliberate mid-video interrupt the playbook prescribes at *"~25â€“35s, where attention drifts."*
- **Track-style references (royalty-free, commercial-safe):**
  - **Epidemic Sound** or **Artlist** â€” search "minimal tech product reveal," "clean confident electronic," ~100â€“110 BPM, no vocals. (Playbook: verify the license *"covers commercial ads with no Content ID claims."*)
  - Free fallback: **Pixabay Music** (CC0-style) or **YouTube Audio Library** filtered to *"Attribution not required"* â€” "corporate minimal tech / ambient drive." Avoid **Uppbeat** unless you paste the per-use credit code (Content ID risk).
- **SFX:**
  - `riser` + `impact` on the 6s SNAP (the single most important sound in the cut).
  - **UI clicks** on each wizard pill advance and the "Run the drop" press â€” keep them crisp and quiet (record clean; don't use the laptop's own click).
  - A soft, satisfying **"secured" chime** on the QR reveal (shot 8) and the green door âœ“ (shot 9).
  - A short, dry **"denied" thunk** on the red DENY card and the red duplicate-scan (shot 9) â€” restrained, never comedic; the scalper losing is fair, not mocked.
  - The Valiron spinner moment (Â§5) plays **near-silent** â€” let the absence build the 1s of suspense before the verdict thunks/chimes in.

---

## 8. CAPTIONS SPEC

- **Why mandatory:** Playbook â€” *"~85% of Facebook video watched muted; ~41% of videos are incomprehensible without sound; burn captions in (don't trust platform auto-CC)."* This video MUST fully land silent.
- **Style:** Monochrome to match brand â€” **white text, heavy/condensed sans (e.g. Inter Tight / Helvetica Now bold), on a subtle black scrim** or with a soft shadow for contrast on the white product UI. No rainbow karaoke styling â€” the brand is *"stark monochrome, premium, minimal."* Key nouns (**ALLOW**, **DENY**, **face value**, **Built on Valiron**) get a weight bump, not a color circus; the âœ“ may use the app's green and the âœ• the app's red to echo the UI.
- **Placement:** **Lower-third for VO captions**, kept inside the 9:16 safe area so the vertical repost doesn't clip them. **The "Not scripted" headline + the ALLOW/DENY sub-labels sit center-screen on the cards** (the eye is already there). Persistent **`facevalue.store`** chip bottom-corner from shot 6.
- **Muted-legibility check (must pass before publishing):** Watch start-to-finish on mute. You must be able to follow the entire argument from on-screen text alone: **$60â†’$1,200 â†’ "Real fans shut out" â†’ "So we built the gate" â†’ 36/40 vs 40/40 Resale:0 â†’ "Not scripted: ALLOW 92 / DENY sandbox" â†’ "One human, one ticket" â†’ "secured at face value, non-transferable" â†’ "scan twice: already used" â†’ "Built on Valiron."** If any link in that chain is unreadable silent, enlarge or extend it. Proofread auto-captions for product nouns â€” force-correct **Valiron**, **World ID**, **ALLOW/DENY**, **face value** (auto-tools mangle these).

---

## 9. ASSETS & GEAR

**Screen-record (the proof core â€” capture at 1440px wide, 60 fps, clean demo account, notifications/bookmarks hidden):**
- **`/simulation`** â€” full run: idle â†’ "Run the drop" â†’ both grids fill â†’ summaries â†’ scroll to the two live cards resolving **ALLOW / DENY**. *(This is the one shot to also capture in 16:9 â€” the split-screen is horizontal by nature; crop a centered 1:1 for the master and keep the wide for a 9:16 stacked version.)*
- **`/drop/[id]`** â€” the **locked onboarding wizard**: the 3 pills (Account / One human / Payment), the *"Prove you're one human"* step, *"You're in â€” one human, one ticket,"* then the live **Buy at face value** claim â†’ success result string.
- **`/tickets`** â€” the issued **QR ticket** (for the macro push).
- **`/door`** â€” staff view: paste a valid token â†’ green **"âœ“ Checked in,"** then paste it **again** â†’ red **"Already checked in â€” duplicate scan."** (Capture both scans in one take.)
- *(Optional)* **`/fan`** â€” the **"valiron Â· engine"** terminal streaming the real call + millisecond timing, for the Â§5 insert.

**Shoot on camera (the hook + texture â€” phone or DSLR, vertical):**
- Hand holding a phone, refreshing a ticket page â†’ **SOLD OUT** (shot 1). Stage this â€” a plain mobile browser is fine; it just needs to read as "a ticket on-sale."
- A resale listing showing **$1,200** (shot 2) â€” a simple framed screen is enough.
- *(Optional alt-hook B-roll)* a desk with multiple monitors for the 0.4s scalper flash. Overshoot: *"unused B-roll is free, missing B-roll forces a bad cut."*

**Solo-friendly tools (from the playbook toolkit):**
- **Screen recording:** Screen Studio (auto-zoom on clicks + cursor smoothing â€” ideal for the wizard/door clicks) or free **Cursorful**/**OBS**. Record native res, **downscale to 1080p on export.**
- **VO:** brother records in a soft, quiet room (rugs/curtains kill echo) on any decent close mic â€” *"audio matters more than video."*
- **Edit + captions:** **CapCut** (fast social cut + free AI captions) or **DaVinci Resolve** (free, SRT). Proofread product nouns.
- **Cursor discipline:** move slowly, **pause before each click so auto-zoom lands** (playbook screen-demo technique).

---

## 10. EDITING & MOTION NOTES

- **The hinge is everything: the 6s SNAP.** Make the cut from pain-world â†’ product-world a **match-cut / whip-pan** so it reads as one continuous push, not an edit (playbook: *"let the product carry the cut... Apple's transitions are 'hard to see where the cut is made'"*). Land the `riser`+`impact` exactly on it. This single transition is what fuses the two halves into "The Fusion."
- **Cut rhythm:** Act 1 fast and urgent (rapid cuts, ~1â€“1.5s shots = urgency). Act 2 alternates **momentum cuts** (wizard pills, click-to-click) with **one held hero beat** â€” the live ALLOW/DENY cards and the QR macro get longer holds to *"let the hero detail breathe."* Vary shot length deliberately.
- **Pattern interrupt every ~3â€“5s** (playbook: *"interrupts ~every 4s averaged 58% retention vs 41%"*): the SNAP (6s) â†’ grids resolving (9s) â†’ scroll-reveal of live cards (13s) â†’ wizard cut (23s) â†’ the **deliberate ~28s music-layer drop** â†’ door greenâ†’red flip (37â€“41s) â†’ logo resolve (41s).
- **Auto-zoom / punch-ins:** 10â€“15% punch-ins on the resale **$1,200**, on **ALLOW 92 / DENY sandbox**, and on the **duplicate-scan** rejection. Cursor smoothing on all UI.
- **Reduce background motion under text:** freeze/slow the grid animation while the live-card captions are up so the eye lands on the verdict.
- **The door beat is the closer's punch** â€” cut the *second* scan rejection (red) on a hard beat with the "denied" thunk; it's the most quotable visual proof of *"un-scalpable"* and the playbook's *"split-screen live test, winner by second 6"* energy applied to the kicker.

---

## 11. PRODUCTION CHECKLIST

**Pre-roll (in order â€” do not skip the warm-up):**
1. **Warm Valiron first.** Run `npm run warm`. The edge proxy is free-tier and **cold-starts 30â€“60s**; if you record the live call cold, the Â§5 spinner just **hangs on camera and destroys the magic moment.** Re-warm if you've been idle >10 min before the live-card take.
2. **Reset to a clean demo state:** `POST /api/dev/reset` (wipes users + reseeds drops/inventory). Do this so the wizard starts at a fresh "Account" step and the `/simulation` counts are clean (36/40, blocked 24, resale 0).
3. **Start the dev server:** `npm run dev`. Confirm `next.config.ts` `serverExternalPackages: ["better-sqlite3"]` is intact (a config change needs a restart or every DB route 500s).
4. **Sanity-fire `npm run prove:valiron`** â€” expect **ALLOW on 25459**, **DENY on 1226/1**. This confirms the gate is live before you roll.
5. **Stage the demo accounts/QR:** pre-create one fan that has claimed a ticket so `/tickets` shows a QR and `/door` has a valid token to scan twice. Have the token on the clipboard for the door take.
6. **Browser hygiene:** hide bookmarks bar, silence notifications, set zoom so captions/UI read large, full-screen the app.
7. **Confirm handles before tagging** (see Â§12) â€” IG `@facevalue_us` vs `facevalue.us` is **UNCONFIRMED**; verify before the end card and the tweet.

**Record settings:**
- Screen-record **1440px wide, 60 fps** (30 fps acceptable for the slow wizard UI), capture native res.
- Camera (hook): vertical, 1080p+, 60 fps if available for the refresh motion.
- **Export the master at 1080Ã—1080 (1:1), plus a 1080Ã—1920 (9:16) crop** from the same timeline; match frame rate; H.264.
- Audio: VO + music + SFX on separate tracks, music ducked under VO, even levels, no clipping.

---

## 12. COMPANION TWEET

> **Post copy:**
> Scalper bots take a $60 ticket and relist it for $1,200. Real fans get shut out.
>
> So we built a ticket bots can't touch: one verified human = one ticket, bound to your identity, un-scalpable.
>
> The trust gate in this video is a **live** call â€” not scripted. Verified fan â†’ ALLOW. Scalper â†’ DENY.
>
> Built on @valiron ðŸ–¤
> Real fans. Real tickets. Face value.
> â†’ facevalue.store
>
> [45s video attached]

> **Tags / handle caveat (VERIFY BEFORE POSTING):**
> - **Valiron + founders** â€” tag the Valiron account and **Vatsa Shah** + **Drew Mailen** in the post (this is their feature; quote-tweet or reply to their feature thread to close the loop). **Confirm each @handle on X before posting** â€” the brief gives names, not verified handles.
> - **Founder handle:** Twitter **@D_phantom_x** (given) â€” post from or credit this.
> - **Instagram:** the brief lists **@facevalue_us** (underscore) AND **facevalue.us** (dot) â€” **UNCONFIRMED/conflicting. Verify which is real before putting it on the end card or in any cross-post.** Do not print an unverified handle on the video.
> - Keep copy compliant: scalpers have "bots"; we are the verified, sanctioned pipeline. No "crypto," no "stealth."

---

## 13. DIFFICULTY & RISK

- **Solo founder time estimate: ~1.5â€“2.5 focused days.**
  - ~0.5 day: warm/reset, capture all screen-records clean (multiple takes of `/simulation` and the `/door` double-scan), shoot the phone hook.
  - ~0.5 day: brother records VO; you pull music/SFX.
  - ~1 day: edit â€” the 6s match-cut SNAP, the live-card highlight choreography, burned-in captions, the muted pass, and exporting both 1:1 and 9:16.
  - Buffer ~0.5 day for the inevitable re-record of the Valiron take.
- **Single biggest failure mode: filming the live Valiron call COLD.** If the edge proxy hasn't been warmed, the Â§5 magic-moment spinner **hangs 30â€“60s on camera** â€” the one beat that proves Valiron is real becomes dead air, and the whole credibility thesis (and the founder feature) collapses. **Guard:** run `npm run warm` immediately before the live-card take, keep the session active, and **shoot that take first while it's hot**; if there's any hang, stop, re-warm, re-roll â€” never try to "speed it up in the edit," because a faked-fast resolve undercuts the literal "Not scripted" claim. Secondary failure mode: caption nouns auto-mangled (Valiron/ALLOW/DENY) â€” proofread every burned-in word.

---

**Files referenced (all real, absolute paths):**
- `C:\Users\aldom\Desktop\antigravity\facevalue\src\app\simulation\page.tsx` â€” the magic moment (live ALLOW/DENY cards, "Not scripted" headline, 36/40 + resale:0 summaries)
- `C:\Users\aldom\Desktop\antigravity\facevalue\src\app\fan\page.tsx` â€” the "valiron Â· engine" terminal + millisecond live call (optional Â§5 insert)
- `C:\Users\aldom\Desktop\antigravity\facevalue\src\app\drop\[id]\page.tsx` â€” locked onboarding wizard, claim, "secured at face valueâ€¦ non-transferable" result
- `C:\Users\aldom\Desktop\antigravity\facevalue\src\components\OnboardingWizard.tsx` â€” verbatim 3 steps (Account / One human / Payment), "Prove you're one human," "You're in â€” one human, one ticket"
- `C:\Users\aldom\Desktop\antigravity\facevalue\src\app\door\page.tsx` + `src\lib\tickets.ts` â€” the door double-scan: `âœ“ Checked in` then `Already checked in â€” duplicate scan.`
- `C:\Users\aldom\Desktop\antigravity\facevalue\src\lib\format.ts` â€” real agent constants: FAN `25459`/ethereum, SCALPER `1226`/solana

---
# C-situational-POV

I now have everything I need, grounded in the real code. The simulation page shows the exact on-screen strings ("Not scripted â€” this is the actual trust gate.", "ALLOW" with "score 92 Â· World ID âœ“", "DENY", "score 0 Â· sandbox"), the /fan terminal shows the real 3-2-1 arm-and-fire beat with millisecond timings, and warm.ts confirms the cold-start warm-up. Here is the complete guide.

---

# FaceValue â€” X Promo Video Production Guide
## CONCEPT C: "POV" (founder-on-the-street, build-in-public)

---

## 1. CONCEPT

- **Name:** "Sold Out for $1,200" â€” *FaceValue POV*
- **One-line vibe:** A solo founder walks the real NYC venue route, names the scalping problem out loud, then opens his phone and shows the fix working live â€” build-in-public, native to the feed, lightly provocative but the provocation only ever lands on *the scalper losing* or *the fan winning fairly*.
- **Optimizes for:** **REACH** (and doubles as evergreen build-in-public content). This is the volume/distribution play, not the polished-credibility play â€” but the over-the-shoulder live Valiron call keeps it honest so it survives the "substance gap" trap (Cluely pitfall: anchor every provocative claim to something defensible).
- **Recommended length:** **38â€“45 seconds.** Playbook: TikTok sweet spot is **21â€“34s** for completion, but you optimize *total watch time*, not raw %, and a story-POV needs room for the walkthrough â€” "a 40s at 60% retention beats a 20s at 70%." 38â€“45s is the longest you go while staying under the 60s "keep most content under 60s" ceiling. Hard cap 45s.
- **Aspect ratio:** **9:16 (1080Ã—1920).** This is shot on a phone, walking, native to the feed â€” Reels/TikTok native fills the screen with no bars. Playbook: "9:16 native for TikTok & Reels." X supports 9:16, and for a reach/POV piece the vertical street-walk *is* the format signal that it's not an ad. (If you also want a dedicated X-feed cut, export a 1:1 1080Ã—1080 secondary â€” but lead with 9:16.)

---

## 2. WHY IT WORKS

- **Cluely playbook â€” tension/relatable failure as the hook, with a built-in villain.** Open on a real failure (a sold-out marquee, scalped to $1,200), not a feature list. "Scalpers vs. real fans" is the villain that travels â€” but unlike rage-bait, the moral frame is positive, so we dodge the "rage-bait hangover" (73% report lower trust in inflammatory brands; for a *trust/identity* brand rage-bait is self-defeating).
- **Founder-as-storyteller = trust + free distribution.** "70% prefer buying when the founder tells the story"; "Go Direct" â€” the personal account is the distribution channel before the brand. A solo CUNY founder on his real street *is* the credibility; it cannot be faked and it reads native.
- **Show, don't tell â€” the claim is provable on camera.** Cluely's claims were disproven and the narrative flipped; ours is anchored to the literal on-screen `ALLOW` / `DENY` from a real Valiron call. "Don't *say* scalper bots get blocked â€” *show* the swarm hit the gate and bounce." The over-the-shoulder phone beat does exactly that.
- **Outcome-preview before the walkthrough earns the watch.** The converting-demo spine: show "done" (a fan's ticket / a `DENY` flash) early, then walk one realistic path. Problem-first demos hold 56% vs 34% for feature-first.

---

## 3. THE HOOK (first 3 seconds)

- **Exact first frame:** Founder mid-stride on a Brooklyn sidewalk, phone selfie-held at chest height, a lit venue marquee / box-office over his shoulder slightly out of focus. He's already walking when the clip starts â€” *something is in motion before the viewer presses anything* (playbook: "something should already be in motion"). No logo card, no intro.
- **On-screen text (burned in, top third):**
  > **This show is sold out.**
  > **Scalped for $1,200.**
- **First spoken words (0â€“2s):** *"This $60 show? Sold out â€” scalped for twelve hundred bucks."*
- **Why it stops the scroll:** It's a **problem hook + in-media-res** open ("So I'm standing here andâ€¦") with the price-gap shock ($60 â†’ $1,200) front-loaded as the payoff. Playbook: kill all preamble (+30â€“40% retention vs "hey guys"), open on a pain point (+~23% vs generic), and pass the **mute test** â€” the two stacked text lines deliver the entire stakes with zero sound.

---

## 4. SHOT-BY-SHOT

Full runtime ~42s. Reference real screens by name. Captions are verbatim and burned in.

| # | Time | Shot / Visual | On-screen caption (verbatim) | Voiceover line (verbatim) |
|---|------|---------------|------------------------------|----------------------------|
| 1 | 0â€“3s | Founder walking, selfie, lit marquee over shoulder. Already in motion. | **This show is sold out.**<br>**Scalped for $1,200.** | "This $60 show? Sold out â€” scalped for twelve hundred bucks." |
| 2 | 3â€“7s | Quick whip-pan to the marquee / "SOLD OUT" box-office sign, then back to his face. | **The bots got there first.** | "Bots swept it in seconds, across dozens of fake accounts. Real fans never had a shot." |
| 3 | 7â€“11s | Push-in on founder, more direct to camera, still walking. | **I'm a solo founder building the fix.** | "I'm one person in Brooklyn building the fix. It's called FaceValue." |
| 4 | 11â€“15s | He reaches a comedy-club door / awning, steps toward it. Hand pushes door. | **Real fans. Real tickets. Face value.** | "One verified human, one ticket â€” bound to you, so it can't be bulk-bought or flipped." |
| 5 | 15â€“19s | OUTCOME PREVIEW â€” match-cut from the door into the **phone screen**: `/drop/[id]` with the locked onboarding wizard overlay visible (Account Â· Verify you're one human Â· Payment). | **Watch it actually work â†’** | "Here's the whole thing, live, on my phone." |
| 6 | 19â€“24s | Over-the-shoulder phone: tap **step 2 "verify you're one human"** â€” World ID / Valiron step. Thumb taps Verify. | **Step 2: prove you're one human.** | "Prove you're real â€” once. We never see who you are, only that you're one person." |
| 7 | 24â€“32s | THE MAGIC MOMENT â€” cut to the **/simulation** live cards (or /fan engine terminal). Two cards resolve: left `ALLOW` green, right `DENY` red. Slow cursor, hold on the verdicts. | **Not scripted â€” the real trust gate.** | "This is a live call to Valiron. Verified fan: allowed. Scalper's bots: denied. Not a mockup â€” that's the actual gate." |
| 8 | 32â€“36s | Back to phone: **Claim** tap â†’ `/tickets` ticket with the HMAC QR code fills the screen. | **One ticket. Bound to you.** | "Fan clears it in milliseconds and gets exactly one ticket â€” at face value." |
| 9 | 36â€“39s | Hard cut to **/door** staff scan: green check on first scan, then the SAME ticket scanned again â†’ "already used." | **Can't be flipped. Built on Valiron.** | "Try to resell it? It's locked to your identity. Scan it twice â€” already used." |
| 10 | 39â€“42s | Founder, outside the club, holds phone up to camera, slight smile. Logo "F" (face-in-letter) + URL lower third. CTA. | **facevalue.store**<br>**Join the drop.** | "Real fans. Real tickets. Face value. Join the drop â€” facevalue.store." |

> Note: place a **deliberate pattern interrupt at ~25â€“32s** (the music drop on the `DENY` flash) â€” exactly where the playbook says attention drifts.

---

## 5. THE MAGIC MOMENT (the live Valiron call)

This is the sponsor deliverable and the credibility anchor â€” it must read as **real** and land in **~6â€“10s** (shot #7, 24â€“32s).

- **Which screen:** Use **`/simulation`** for the cleanest two-verdict read (recommended), or the **`/fan`** engine terminal if you want the millisecond drama. Both fire **real** calls. Recommendation: lead with `/simulation`'s two live cards because the side-by-side `ALLOW` vs `DENY` is instantly legible muted; optionally intercut one second of the `/fan` terminal's `(NNms)` timing for the "milliseconds" line.
- **What's actually on screen (do not paraphrase â€” these strings are real):**
  - Section header: **"Live Â· real calls to Valiron"** â†’ **"Not scripted â€” this is the actual trust gate."**
  - Left card "Verified fan's agent": **`âœ“ ALLOW`**, `agentId 25459 Â· ethereum`, **score 92**, route, badge **`World ID âœ“`**, reason line "Human-agent link verified via World ID."
  - Right card "Scalper swarm agent": **`âœ• DENY`**, `agentId 1226`, **score 0**, route **`sandbox`**.
- **How to stage it for legibility:**
  1. **Warm Valiron first** (see checklist) so the cards resolve in <2s on camera â€” never film this cold or it hangs and the magic dies.
  2. Hit **Run the drop**; let both cards finish loading *before* you start narrating the verdict.
  3. **Slow cursor, pause, then auto-zoom** punches into the left `ALLOW`, hold ~1.5s, whip to the right `DENY`, hold ~1.5s (playbook: move cursor deliberately, pause before each click so auto-zoom lands; reduce background motion when text appears).
  4. Add a small **burned-in arrow/box** highlighting the `ALLOW` and `DENY` words and the `World ID âœ“` badge so the eye lands there muted.
- **The caption that sells the realness:** keep the product's own line on screen â€” **"Not scripted â€” this is the actual trust gate."** â€” and add a small persistent corner tag: **"live call Â· Built on Valiron."**

---

## 6. FULL VOICEOVER SCRIPT

> **Tone/delivery direction:** Founder's brother narrates â€” confident, conversational, a little tired-of-the-BS but *not* angry; speak slightly slow with a beat of silence before "This is a live call to Valiron." Peer-to-peer, never salesy. (Playbook: confident/conversational/neutral, VO sets the emotional tempo.)

"This $60 show? Sold out â€” scalped for twelve hundred bucks.
Bots swept it in seconds, across dozens of fake accounts. Real fans never had a shot.
I'm one person in Brooklyn building the fix. It's called FaceValue.
One verified human, one ticket â€” bound to you, so it can't be bulk-bought or flipped.
Here's the whole thing, live, on my phone.
Prove you're real â€” once. We never see who you are, only that you're one person.
This is a live call to Valiron. Verified fan: allowed. Scalper's bots: denied. Not a mockup â€” that's the actual gate.
Fan clears it in milliseconds and gets exactly one ticket â€” at face value.
Try to resell it? It's locked to your identity. Scan it twice â€” already used.
Real fans. Real tickets. Face value. Join the drop â€” facevalue.store."

*(~95 words â‰ˆ 40â€“42s at a slightly-slow conversational pace.)*

---

## 7. MUSIC & SOUND DESIGN

- **Direction:** Single rhythmically-consistent bed, **ducked âˆ’12 to âˆ’15 dB under the VO**, levels even (support, never compete). One **emotion-first audio cue leads**: a low, restrained pulse under the $1,200 hook (urgency/tension) â†’ on the `DENY` flash (~25â€“28s) a **clean low impact + the bed opens up / resolves** to signal "the fix landed." End on one warm tail under the CTA.
- **Track-style references (royalty-free, commercial-safe):**
  - **Pixabay Music** (CC0-style, no attribution) â€” search "minimal tense pulse / dark electronic underscore" for the build; or
  - **Uppbeat** "confident minimal / clean tech" bed (must paste the unique credit code per use to avoid Content ID).
  - Paid upgrade if budget allows: **Epidemic Sound** or **Artlist** "tense-to-uplift" cue (verify the license covers commercial ads with no Content ID claims).
- **SFX:**
  - **Subtle riser** 3s into shots #6â†’#7 building to the verdict.
  - **Single low impact** on the `DENY` (the pattern-interrupt beat).
  - **Soft UI tick** on each phone tap (verify, claim) and a **light "ding"** on the green door-scan check; a flatter "buzz" on the "already used" second scan.
  - **Crisp branded tap/sting** *before* the very first word (playbook: sonic pattern break before words helps the hook).

---

## 8. CAPTIONS SPEC

- **Style:** Stark monochrome to match brand â€” **white text, heavy/condensed sans, on a translucent black pill or with a subtle black drop-shadow** for contrast against street and screen. ALL-CAPS or sentence-case bold; **keyword highlight** in the brand line (e.g., color-pop only on "$1,200", "DENIED", "FACE VALUE" if you want one accent â€” otherwise pure B/W to stay premium).
- **Placement:** Hook lines **top third** (so the founder's face/marquee stays clear); walkthrough captions **lower third**; keep captions **clear of the bottom ~12%** safe area (Reels/TikTok UI). During the phone/`/simulation` shots, shrink captions and move them off the verdict cards so the `ALLOW`/`DENY` stays unobstructed.
- **Burn them in** â€” do not rely on platform auto-CC (renders inconsistently). Narrate all key info as on-screen text.
- **Muted-legibility check (must pass):** Watch start-to-finish on silent. The story must be fully readable: **(1)** "Sold out â€” scalped $1,200" â†’ **(2)** "The bots got there first" â†’ **(3)** "Solo founder building the fix" â†’ **(4)** the phone wizard + "prove you're one human" â†’ **(5)** green `ALLOW` / red `DENY` with "Not scripted â€” the real trust gate" â†’ **(6)** QR ticket "bound to you" â†’ **(7)** door scan "already used" â†’ **(8)** "facevalue.store / Join the drop." If any beat is unintelligible muted, add/extend a caption. (Playbook: ~85% watch muted; captioned video is 80% more likely to be finished.)

---

## 9. ASSETS & GEAR

**Screen-record (on the founder's phone, real app):**
- `/drop/[id]` with the **locked onboarding wizard** overlay visible (Account â†’ verify one human â†’ Payment).
- The **step 2 verify** tap (World ID / Valiron).
- `/simulation` â€” **Run the drop** â†’ the two live verdict cards resolving to `ALLOW` (25459, score 92, World ID âœ“) and `DENY` (1226, score 0, sandbox). *(Primary magic-moment capture.)*
- Optional: `/fan` engine terminal â€” the 3-2-1 "DROP OPEN â€” agent firing" + `(NNms)` timing line, for the "milliseconds" beat.
- **Claim â†’ `/tickets`** QR ticket.
- `/door` staff scan â€” green check, then same ticket again â†’ "already used."

**Shoot on camera (phone, handheld OK â€” native is the point):**
- Walking selfie on the real NYC venue route (marquee / box-office in frame).
- The whip-pan to a "SOLD OUT" sign.
- Hand pushing the comedy-club door (the match-cut into the phone).
- Founder holding the phone up at the end + logo/URL lower third.

**Solo-friendly tools:**
- **Phone screen capture:** built-in iOS/Android recorder at **60fps**, native res. If recording the app on desktop instead, **screen-record ~1440px wide** then crop to 9:16 (per production realities); use **Screen Studio / Cursorful** for auto-zoom + cursor smoothing on the `/simulation` capture.
- **Audio:** record the VO separately in a soft, quiet room (curtains/rugs kill echo) â€” a close mic in a treated room beats a fancy mic in a bare one. Capture some **street ambience** for texture under the walking shots.
- **Stabilization:** phone gimbal or just steady two-hand grip; the slight handheld read is fine and on-brand for build-in-public.
- **Edit + caption (free):** **CapCut** (fast social cut + AI captions, animated styles) or **DaVinci Resolve** (master edit + SRT). **Proofread auto-captions** for "Valiron," "FaceValue," "$1,200," "World ID," "DENY."
- **Export:** 1080Ã—1920, matching frame rate, downscaled to 1080p.

---

## 10. EDITING & MOTION NOTES

- **Cut rhythm:** Pattern interrupt **every 3â€“5s** â€” angle change, whip-pan, push-in, the phone match-cut, the verdict zoom (playbook: interrupts ~every 4s â†’ 58% vs 41% retention).
- **The hero transition:** **match-cut the door-push (shot #4) into the phone screen (shot #5)** â€” share the motion vector / dark doorway shape so the camera reads as one continuous push into the app (playbook: "let the product carry the cut," wormhole-style â€” hard to see where the cut is).
- **Punch-ins:** 10â€“15% zooms on the founder's face at "I'm one personâ€¦ building the fix," and auto-zoom on the `ALLOW`/`DENY` words.
- **Cut to the beat / cut on motion:** edit the walking strides and taps to the music pulse; **rapid cuts** through the bots/problem section (urgency), **longer hold** (~1.5s each) on the two verdict cards so the proof breathes.
- **The deliberate ~25â€“32s interrupt:** the music opening up + the red `DENY` impact is your mid-clip re-grab, placed exactly where attention drifts.
- **Trim micro-pauses / dead frames** between every cut so momentum never sags. Reduce background motion whenever a caption is on screen.
- **Cold-open discipline:** no logo until the very end â€” first frame is the founder + stakes.

---

## 11. PRODUCTION CHECKLIST (ordered)

**Pre-roll (do in order, before any screen capture):**
1. **Warm Valiron** â€” run `npm run warm` (wakes the Render edge proxy + primes agents 25459 / 1226). The proxy cold-starts **30â€“60s**; if filmed cold the live call **hangs on camera and ruins the moment.** Confirm console prints both agents' routes before filming.
2. **Reset to clean demo state** â€” `POST /api/dev/reset` (wipes/reseeds so inventory is full and no prior ticket pollutes `/door`).
3. Re-run **`npm run warm`** a final time ~2 min before the `/simulation` take if there's been a long gap (free tier can re-sleep).
4. Start `npm run dev`; load `/drop/[id]`, `/simulation`, `/tickets`, `/door` in tabs and pre-scroll each so the hero element is framed.
5. **Hide notifications, bookmarks, extra tabs**; use the clean seeded demo account; phone on Do-Not-Disturb (no banners mid-capture).
6. Dry-run the `/simulation` "Run the drop" once to confirm `ALLOW`/`DENY` resolve fast; dry-run the `/door` double-scan so you know the "already used" timing.

**Record settings:**
- **9:16, 1080Ã—1920**, **60fps**, native resolution capture â†’ downscale to 1080p on export.
- Desktop screen captures at **~1440px wide**, then crop to 9:16.
- VO recorded separately, quiet treated room; capture street ambience.
- Overshoot all B-roll (extra seconds head/tail per shot for clean in/out points).

**Pre-publish:**
- Muted-legibility pass (Section 8).
- Proofread captions for product/sponsor names.
- Confirm the corner "Built on Valiron" tag is present during the magic moment.

---

## 12. COMPANION TWEET

> **Post copy:**
> A $60 ticket. Sold out. Scalped for $1,200.
>
> I'm a solo founder in Brooklyn building the fix: **FaceValue** â€” one verified human, one ticket, bound to your identity so it can't be bulk-bought or flipped.
>
> That trust gate in the video? A **real, live call** â€” verified fan ALLOWED, scalper bots DENIED. Not a mockup.
>
> Real fans. Real tickets. Face value.
> Built on @valiron ðŸŸ¢
> â†’ facevalue.store
>
> **Tags:** `@valiron` (Valiron â€” the feature you're delivering to) Â· founder handle **@D_phantom_x**.

> **HANDLE CAVEAT (verify before posting):**
> - **Confirm Valiron's exact X handle** (and Vatsa Shah / Drew Mailen's handles) before tagging â€” don't tag a wrong/parody account on a sponsor deliverable.
> - **IG handle is UNCONFIRMED:** given as **@facevalue_us** (underscore) but **facevalue.us** (dot) elsewhere â€” do **not** include the IG handle until you've confirmed which one resolves. Twitter founder handle **@D_phantom_x** is the safe tag.
> - Keep "**Built on Valiron**" credit in the copy regardless (language-discipline rule).

---

## 13. DIFFICULTY & RISK

- **Solo founder time estimate:** **~6â€“9 hours** end-to-end.
  - Warm/reset + screen captures (`/drop`, `/simulation`, `/tickets`, `/door`): ~1.5h (with retakes).
  - Street B-roll on the venue route: ~1.5â€“2h (travel + a few walking takes + the door match-cut).
  - VO record (brother): ~0.5h.
  - Edit + match-cut + captions + music + muted pass: ~2.5â€“4h.
- **Single biggest failure mode to guard against:** **The live Valiron call filmed cold = it hangs on camera and the credibility centerpiece dies.** The Render proxy cold-starts 30â€“60s. **Mitigation:** run `npm run warm` immediately before the `/simulation` take, confirm both agents resolve in the console, and re-warm if any long gap precedes the take â€” and capture a clean backup take of the cards resolving fast. (Second-order risk: tagging a wrong sponsor/IG handle â€” see Section 12 caveat. Third: rage-bait drift â€” keep every edgy beat pointed at *the scalper losing / the fan winning fairly*, never a covert-advantage angle, per the language discipline.)

---

**Files referenced (all real, verified this session):**
- `C:\Users\aldom\Desktop\antigravity\facevalue\src\app\simulation\page.tsx` â€” live `ALLOW`/`DENY` verdict cards, "Not scripted â€” this is the actual trust gate." (the magic moment).
- `C:\Users\aldom\Desktop\antigravity\facevalue\src\app\fan\page.tsx` â€” `valiron Â· engine` terminal, 3-2-1 arm-and-fire, millisecond timing, QR ticket.
- `C:\Users\aldom\Desktop\antigravity\facevalue\scripts\warm.ts` â€” the `npm run warm` cold-start warm-up (agents 25459 / 1226).
